import "./theme.css";
import "@coinbase/onchainkit/styles.css";
import type { Metadata, Viewport } from "next";
import "./globals.css";
import { Providers } from "./providers";

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export async function generateMetadata(): Promise<Metadata> {
  const URL = process.env.NEXT_PUBLIC_URL;
  return {
    title: "Vibe Portfolio",
    applicationName: "Vibe Portfolio",
    description: "Vibe Portfolio — Crypto prices and portfolio tracking Mini App",
    openGraph: {
      siteName: "Vibe Portfolio",
      title: "Vibe Portfolio",
      description: "Crypto prices and portfolio tracking Mini App",
    },
    other: {
      "fc:frame": JSON.stringify({
        version: "next",
        imageUrl: process.env.NEXT_PUBLIC_APP_HERO_IMAGE,
        button: {
          title: "Launch Vibe Portfolio",
          action: {
            type: "launch_frame",
            name: "Vibe Portfolio",
            url: URL,
            splashImageUrl: process.env.NEXT_PUBLIC_SPLASH_IMAGE,
            splashBackgroundColor:
              process.env.NEXT_PUBLIC_SPLASH_BACKGROUND_COLOR,
          },
        },
      }),
      "apple-mobile-web-app-title": "Vibe Portfolio",
      "application-name": "Vibe Portfolio",
    },
  };
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="bg-background">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
